# Runtime deps for the React desk's FastAPI backend (webapp/server.py), used by the
# container image. This is the closure the server + its XVA seam actually import; the
# heavier engine extras (numba/duckdb/jax/matplotlib/sqlalchemy) are not on this path.
fastapi>=0.110
uvicorn[standard]>=0.27
numpy>=1.26
scipy>=1.12
pandas>=2.2
pyarrow>=15.0
pydantic>=2.6
jinja2>=3.1
requests>=2.31
certifi>=2024.2
